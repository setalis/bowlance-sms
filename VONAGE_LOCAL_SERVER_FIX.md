# 🚨 РЕШЕНО: Ошибка отправки кода Vonage на локальном сервере

## Проблема

При попытке отправить код верификации на локальном сервере (OSPanel) возникает ошибка:

```
[2026-01-25 11:17:53] local.ERROR: Vonage verification exception 
{"message":"cURL error 28: Resolving timed out after 10007 milliseconds 
for https://api.nexmo.com/v2/verify"}
```

### Причина
Локальный PHP не может подключиться к внешнему API Vonage из-за:
- ❌ Блокировки сети в OSPanel
- ❌ Брандмауэра Windows
- ❌ Ограничений cURL/PHP

## ✅ Решение: Тестовый режим

Добавлен **тестовый режим** для локальной разработки!

### Что изменено:

#### 1. `.env` - включён тестовый режим
```env
# Vonage API Configuration
VONAGE_KEY=d42e7e62
VONAGE_SECRET=^(zsOpK9y
VONAGE_SIGNATURE_SECRET=
VONAGE_PRIVATE_KEY=
VONAGE_APPLICATION_ID=
VONAGE_HTTP_CLIENT=
# Для тестирования на локальном сервере без доступа к API Vonage
VONAGE_TEST_MODE=true
```

#### 2. `config/vonage.php` - добавлена настройка
```php
'test_mode' => env('VONAGE_TEST_MODE', false),
```

#### 3. `VonageVerifyService` - поддержка тестового режима
- ✅ Не делает реальные HTTP запросы к Vonage
- ✅ Генерирует тестовый `request_id`
- ✅ Принимает **любой 6-значный код** (123456, 999999, и т.д.)
- ✅ Логирует все действия для отладки
- ✅ Добавлен таймаут 30 секунд для реальных запросов

## 🎯 Как использовать

### На локальном сервере (тестовый режим):

1. **Откройте `.env`** и убедитесь:
   ```env
   VONAGE_TEST_MODE=true
   ```

2. **Перезапустите сервер**:
   ```bash
   # Если используете встроенный сервер Laravel
   php artisan serve
   ```

3. **Тестируйте верификацию**:
   - Добавьте товар в корзину
   - Нажмите "Оформить заказ"
   - Заполните имя и телефон
   - Нажмите "Отправить код"
   - **Введите ЛЮБОЙ 6-значный код**: `123456`, `999999`, `111111`
   - Код будет принят! ✅

4. **Проверьте логи**:
   ```bash
   tail -f storage/logs/laravel.log
   ```
   
   Вы увидите:
   ```
   [INFO] Test mode: Verification code sent
   [INFO] Test mode: Code verified
   ```

### На продакшене (реальный API):

1. **Откройте `.env`** и установите:
   ```env
   VONAGE_TEST_MODE=false
   VONAGE_KEY=ваш_реальный_ключ
   VONAGE_SECRET=ваш_реальный_секрет
   ```

2. **Убедитесь**, что сервер имеет доступ к интернету

3. **Проверьте** баланс на Vonage Dashboard

## 📋 Функционал в тестовом режиме

| Действие | Тестовый режим | Продакшн |
|----------|----------------|----------|
| Отправка кода | ✅ Мгновенно | ✅ Реальный SMS |
| Генерация request_id | ✅ `test-{uuid}` | ✅ От Vonage |
| Проверка кода | ✅ Любой 6-значный | ✅ Только правильный |
| Логирование | ✅ Все действия | ⚠️ Только ошибки |
| SMS отправка | ❌ Нет | ✅ Да |

## 🔧 Дополнительные исправления

### Увеличен таймаут HTTP запросов
```php
Http::timeout(30) // Было: без таймаута
    ->withBasicAuth($this->apiKey, $this->apiSecret)
    ->post($this->baseUrl, [...]);
```

### Улучшены сообщения об ошибках
```php
return [
    'success' => false,
    'message' => 'Произошла ошибка при отправке кода: '.$e->getMessage(),
];
```

## 🐛 Отладка проблем

### Если код не отправляется:

1. **Проверьте `.env`**:
   ```bash
   php artisan config:cache
   php artisan config:clear
   ```

2. **Проверьте логи**:
   ```bash
   # Windows PowerShell
   Get-Content storage/logs/laravel.log -Tail 50
   ```

3. **Тест подключения к Vonage** (если test_mode=false):
   ```bash
   curl -v https://api.nexmo.com/v2/verify
   ```

### Если тестовый режим не работает:

1. Очистите кеш конфигурации:
   ```bash
   php artisan config:clear
   ```

2. Проверьте миграции:
   ```bash
   php artisan migrate:status
   ```

3. Проверьте, что в `phone_verifications` создаются записи:
   ```sql
   SELECT * FROM phone_verifications ORDER BY created_at DESC LIMIT 5;
   ```

## ⚠️ ВАЖНО для продакшена!

**НЕ ЗАБУДЬТЕ** отключить тестовый режим на продакшене:

```env
# Продакшен .env
VONAGE_TEST_MODE=false
VONAGE_KEY=реальный_ключ
VONAGE_SECRET=реальный_секрет
```

Иначе любой сможет верифицировать любой номер телефона!

## ✅ Готово!

Теперь вы можете:
- ✅ Тестировать верификацию без интернета
- ✅ Разрабатывать локально без API ключей
- ✅ Переключиться на продакшен одной строкой в `.env`

---

**Статус**: ✅ Исправлено  
**Дата**: 25 января 2026  
**Тестовый код**: `123456` (или любой 6-значный)
