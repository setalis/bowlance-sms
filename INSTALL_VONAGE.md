# 🚀 Установка и настройка Vonage для верификации телефонов

## Шаг 1: Исправление проблемы с временной директорией PHP

У вас возникает ошибка:
```
A temporary file could not be opened to write the process output: 
fopen(H:/OSPanel/temp/PHP-8.3/default\sf_proc_0C.out.lock): 
Failed to open stream: Permission denied
```

### Решение A: Создать директорию вручную

Откройте командную строку с правами администратора и выполните:

```bash
mkdir H:\OSPanel\temp\PHP-8.3\default
```

Или создайте папку через Проводник Windows:
1. Откройте `H:\OSPanel\temp\`
2. Создайте папку `PHP-8.3` (если нет)
3. Внутри неё создайте папку `default`
4. Убедитесь, что у папки есть права на запись

### Решение B: Изменить настройку в php.ini

1. Откройте файл `php.ini` (обычно в `H:\OSPanel\data\PHP-8.3\default\php.ini`)
2. Найдите строку с `sys_temp_dir`
3. Измените на:
```ini
sys_temp_dir = "C:\Windows\Temp"
```
4. Сохраните и перезапустите PHP/Apache

## Шаг 2: Установка пакета Vonage

После исправления проблемы с temp директорией:

```bash
composer require vonage/vonage-laravel
```

### Если возникают конфликты с Guzzle

Некоторые пакеты могут конфликтовать с Guzzle. В этом случае используйте альтернативный подход:

```bash
composer require vonage/client-core
composer require symfony/http-client php-http/message-factory php-http/httplug nyholm/psr7
```

Затем добавьте в `.env`:
```env
VONAGE_HTTP_CLIENT="Symfony\\Component\\HttpClient\\HttplugClient"
```

## Шаг 3: Получение API ключей Vonage

1. Перейдите на https://dashboard.nexmo.com/
2. Создайте аккаунт или войдите
3. В дашборде найдите раздел "API Settings" или "Getting Started"
4. Скопируйте:
   - API Key
   - API Secret

## Шаг 4: Настройка .env файла

Откройте файл `.env` и заполните параметры Vonage:

```env
VONAGE_KEY=your_api_key_here
VONAGE_SECRET=your_api_secret_here
```

Остальные параметры можно оставить пустыми (они нужны только для Voice API):
```env
VONAGE_SIGNATURE_SECRET=
VONAGE_PRIVATE_KEY=
VONAGE_APPLICATION_ID=
```

## Шаг 5: Проверка работы

### 5.1. Проверка конфигурации

```bash
php artisan tinker
```

В tinker выполните:
```php
config('vonage.api_key');
config('vonage.api_secret');
```

Должны вернуться ваши ключи.

### 5.2. Запуск тестов

```bash
# Тесты верификации телефона
php artisan test --filter=PhoneVerificationTest

# Тесты заказов с верификацией
php artisan test --filter=OrderWithPhoneVerificationTest

# Все тесты
php artisan test
```

## Шаг 6: Тестовая отправка кода

Создайте временный маршрут для проверки (удалите после теста):

```php
// routes/web.php
Route::get('/test-vonage', function () {
    $service = app(\App\Services\VonageVerifyService::class);
    $result = $service->sendVerificationCode('+995555123456'); // Ваш реальный номер
    dd($result);
});
```

Откройте в браузере: `https://bowlance.fly/test-vonage`

Если всё настроено правильно, вы получите:
```php
array:2 [
  "success" => true
  "request_id" => "c4a12345..."
]
```

И SMS с кодом на указанный номер.

**Не забудьте удалить тестовый маршрут после проверки!**

## Шаг 7: Пополнение баланса (для продакшена)

1. Войдите в Vonage Dashboard
2. Перейдите в раздел "Billing"
3. Пополните баланс (тестовый аккаунт часто даёт бесплатные кредиты)
4. Проверьте тарифы для вашего региона: https://www.vonage.com/communications-apis/sms/pricing/

### Примерные цены (на январь 2026):
- Грузия (995): ~0.04-0.05 EUR за SMS
- Россия (7): ~0.02-0.03 EUR за SMS
- Другие страны: см. официальный прайс

## Устранение неполадок

### Ошибка: "Invalid credentials"
- Проверьте правильность API ключей в `.env`
- Убедитесь, что нет лишних пробелов
- Попробуйте скопировать ключи заново из Dashboard

### Ошибка: "Insufficient balance"
- Пополните баланс в Vonage Dashboard
- Проверьте, что аккаунт активирован

### Ошибка: "Invalid phone number"
- Номер должен быть в международном формате: `+995XXXXXXXXX`
- Убедитесь, что код страны правильный

### SMS не приходит
1. Проверьте баланс аккаунта
2. Убедитесь, что номер в правильном формате
3. Проверьте, что страна поддерживается Vonage
4. Посмотрите логи: `storage/logs/laravel.log`

### Ошибка при запуске тестов
Если тесты не запускаются из-за temp директории:
- Убедитесь, что вы исправили проблему из Шага 1
- Перезапустите терминал/командную строку
- Попробуйте запустить от имени администратора

## Проверочный список

- [ ] Создана/настроена temp директория PHP
- [ ] Установлен пакет `vonage/vonage-laravel`
- [ ] Получены API ключи из Vonage Dashboard
- [ ] Ключи добавлены в `.env`
- [ ] Тесты запускаются успешно
- [ ] Тестовая отправка SMS работает
- [ ] Пополнен баланс (для продакшена)
- [ ] Интегрирован UI на фронтенде

## Готово к работе! 🎉

После выполнения всех шагов ваша система верификации телефонов полностью готова к использованию.

### Следующие шаги:
1. Прочитайте `VONAGE_INTEGRATION.md` для деталей API
2. Посмотрите примеры в `public/js/phone-verification-example.js`
3. Интегрируйте верификацию в форму оформления заказа
4. Протестируйте полный процесс оформления заказа

## Полезные ссылки

- 📚 [Полная документация](./VONAGE_INTEGRATION.md)
- 🚀 [Быстрый старт](./QUICK_START_VONAGE.md)
- 📝 [Сводка изменений](./VONAGE_CHANGES_SUMMARY.md)
- 🌐 [Vonage Dashboard](https://dashboard.nexmo.com/)
- 📖 [Vonage API Docs](https://developer.vonage.com/en/api/verify.v2)

## Поддержка

Если у вас возникли проблемы:
1. Проверьте логи: `storage/logs/laravel.log`
2. Посмотрите документацию Vonage
3. Проверьте тесты: они показывают правильное использование API
